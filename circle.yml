
general:
  branches:
    only:
      - master # for now, only build the master branch
machine:
  services:
    - docker
dependencies:
  override:
    # build the GoDemo binary
    - go get -t -d -v ./...
    - make build
    # create and export as a docker image
    - docker build -t gotrygo:0.1.0 .
    - docker save gotrygo:0.1.0 > $CIRCLE_ARTIFACTS/gotrygo-0.1.0.tar
test:
  override:
    # insert all test cases
    - echo "Finished"
